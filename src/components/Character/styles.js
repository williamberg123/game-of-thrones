import styled from 'styled-components';

const StyledCharacter = styled.div`
	display: flex;
	padding: 10px;
	border-radius: 5px;

	& > img {
		width: 50%;
		margin-right: 20px;
	}
`;

export default StyledCharacter;
